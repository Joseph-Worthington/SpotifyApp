import React from 'react';

const SearchBar = () => {
    return (
        <section>
            <input type="text" />
            <button>Search</button>
        </section>
    );
}

export default SearchBar;